import express from "express";

const router = express.Router();

// Abhi ke liye is router ko khali rakhte hain ya zaroori routes add karein jab aap taiyaar hon.
// Example: router.get("/", (req, res) => res.send("Delegator router is active"));

export default router;