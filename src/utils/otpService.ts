export const sendOtpToEmail = async (email: string, otp: string) => {
    // You can integrate nodemailer, SendGrid, etc.
    console.log(`Sending OTP ${otp} to email: ${email}`);
    // Add actual email sending logic here
  };
  